defmodule Katalyst.ReloaderTest do
  use ExUnit.Case

  test "This should have tests" do
    IO.puts "test's missing"
    # setup start the genserver/sup/whatev
    # n directories to get the most recent mtime
    # touch a file
    # do a new scan and assert code is reloaded
  end
end
